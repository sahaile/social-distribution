{
  "devDependencies": {
    "esbuild": "^0.25.5",
    "marked": "^15.0.12"
  },
  "standard": {
    "env": [
      "browser"
    ]
  },
  "dependencies": {
    "commonmark": "^0.31.2",
    "css-validator": "^0.11.0",
    "dompurify": "^3.2.6"
  },
  "scripts": {
    "build": "npx esbuild ./webapp/renderer.js --bundle --minify --sourcemap --format=esm --outfile=./static/renderer.min.js",
    "postinstall": "npm run build"
  }
}
